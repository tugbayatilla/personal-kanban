{
  "name": "@personal-kanban/web",
  "version": "1.0.0",
  "description": "Local web app for personal-kanban board",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "jest --config jest.config.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@personal-kanban/core": "*",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/jest": "*",
    "@types/node": "*",
    "@types/supertest": "^6.0.0",
    "jest": "*",
    "supertest": "^6.3.0",
    "ts-jest": "*",
    "typescript": "*"
  }
}
